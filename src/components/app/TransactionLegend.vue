<template>
  <div class="flex items-center gap-3 sm:gap-4 text-xs font-jakarta flex-wrap">
    <!-- Pemasukan -->
    <div class="inline-flex items-center gap-1.5 text-gray-600 select-none">
      <span class="w-2 h-2 rounded-full bg-emerald-500 flex-shrink-0"></span>
      <span class="text-[11px] sm:text-xs">Pemasukan</span>
    </div>

    <!-- Pengeluaran -->
    <div class="inline-flex items-center gap-1.5 text-gray-600 select-none">
      <span class="w-2 h-2 rounded-full bg-rose-500 flex-shrink-0"></span>
      <span class="text-[11px] sm:text-xs">Pengeluaran</span>
    </div>

    <!-- Transfer -->
    <div class="inline-flex items-center gap-1.5 text-gray-600 select-none">
      <span class="w-2 h-2 rounded-full bg-blue-500 flex-shrink-0"></span>
      <span class="text-[11px] sm:text-xs">Transfer</span>
    </div>
  </div>
</template>

<script setup>
// Shared transaction legend component for Dashboard & Laporan
</script>
